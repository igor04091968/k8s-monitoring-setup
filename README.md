# Kubernetes Monitoring and Application Setup

Этот репозиторий содержит конфигурационные файлы и скрипты автоматизации для развертывания кластера `k3s` с стеком мониторинга Prometheus и Grafana, экспортером WireGuard, а также приложения `sui`. Он предназначен для локальной установки на ноутбуке.

---

## 🚀 Предварительные требования

Для работы с этим набором конфигураций вам потребуются следующие инструменты:

*   **`k3s`**: Легковесный дистрибутив Kubernetes.
*   **`Helm`**: Менеджер пакетов для Kubernetes.
*   **`kubectl`**: Инструмент командной строки для работы с кластером Kubernetes.
*   **`Ansible`**: Инструмент автоматизации для развертывания экспортера WireGuard на удаленных серверах.

## 🗂️ Обзор файлов и директорий

*   `Monitoring_Instruction.md`: Подробное руководство по установке `k3s` и стека мониторинга.
*   `values-light.yaml`: Файл конфигурации Helm для Prometheus-оператора и Grafana, оптимизированный для локального развертывания.
*   `grafana-ingress.yaml`: Ресурс Kubernetes Ingress для доступа к Grafana по доменному имени.
*   `wireguard-dashboard.json`: JSON-файл дашборда Grafana для мониторинга WireGuard.
*   `connect_grafana.sh`: Скрипт для автоматического проброса порта к Grafana для локального доступа.
*   `sui-deployment.yaml`: Определение Kubernetes Deployment для приложения `sui`.
*   `sui-service.yaml`: Определение Kubernetes Service (NodePort) для приложения `sui`.
*   `ansible/`: Директория с Ansible-плейбуками.
    *   `ansible/install_wireguard_exporter.yml`: Плейбук для установки WireGuard-экспортера на удаленные серверы.
    *   `ansible/templates/wireguard_exporter_binary.service.j2`: Шаблон systemd-сервиса для WireGuard-экспортера.

---

## 🛠️ Шаги по развертыванию

Следуйте этим шагам для развертывания кластера и всех компонентов.

### **Шаг 1: Установка `k3s` и `Helm`**

Следуйте инструкциям из файла `Monitoring_Instruction.md` для начальной установки `k3s` и `Helm`.

### **Шаг 2: Развертывание стека мониторинга (Prometheus и Grafana)**

Примените конфигурацию `values-light.yaml` для установки Prometheus-оператора и Grafana в кластер.

```bash
sudo KUBECONFIG=/etc/rancher/k3s/k3s.yaml helm upgrade --install prometheus prometheus-community/kube-prometheus-stack -n monitoring --create-namespace -f values-light.yaml
```

### **Шаг 3: Развертывание WireGuard Exporter**

Этот шаг использует Ansible для установки `prometheus-wireguard-exporter` на ваши удаленные серверы.

1.  Перейдите в директорию `ansible` внутри этого репозитория:
    ```bash
    cd ansible/
    ```
2.  Запустите плейбук:
    ```bash
    ansible-playbook install_wireguard_exporter.yml
    ```
    (Используйте флаг `-K`, если для `sudo` на удаленных серверах требуется пароль).
3.  Вы можете проверить доступность экспортера, выполнив команду (должен вернуть список метрик):
    ```bash
    curl http://<IP_вашего_удаленного_сервера>:9586/metrics
    ```

### **Шаг 4: Развертывание приложения `sui`**

Разверните приложение `sui` в кластере.

```bash
kubectl apply -f sui-deployment.yaml
kubectl apply -f sui-service.yaml
```

### **Шаг 5: Публикация Grafana через Ingress**

Создайте Ingress-ресурс для доступа к Grafana по доменному имени.

```bash
sudo kubectl apply -f grafana-ingress.yaml
```

---

#### **⚠️ Важно: Настройка внешнего доступа к Grafana (DNS и Port Forwarding)**

После применения Ingress-ресурса для Grafana, необходимо выполнить следующие шаги, чтобы сделать Grafana доступной из интернета:

1.  **Настройка проброса портов на вашем роутере:**
    Вам необходимо войти в административную панель вашего домашнего роутера и настроить проброс портов:
    *   **HTTP (порт 80):** Пробросить внешний порт `80` (TCP) на **внутренний IP-адрес вашего ноутбука** (например, `192.168.0.7`) на порт `32600`.
    *   **HTTPS (порт 443):** Пробросить внешний порт `443` (TCP) на **внутренний IP-адрес вашего ноутбука** (например, `192.168.0.7`) на порт `30701`.

    Эти порты (`32600` и `30701`) используются Ingress-контроллером Traefik в `k3s` для прослушивания HTTP/HTTPS трафика.

2.  **Настройка DNS A-записи:**
    В панели управления вашего DNS-провайдера (где вы управляете `dpdns.org`) создайте или обновите **A-запись** для `notebook.iri1968.dpdns.org`. Эта запись должна указывать на **публичный IP-адрес вашего ноутбука**.

    *   Вы можете узнать свой текущий публичный IP-адрес, набрав "what is my ip" в поиске Google.
    *   Учтите, что публичный IP-адрес может меняться, и при его смене вам потребуется обновить A-запись.

После выполнения этих шагов и распространения DNS-изменений, Grafana будет доступна по адресу `http://notebook.iri1968.dpdns.org`.

---

### **Шаг 6: Импорт дашборда WireGuard в Grafana**

1.  Откройте Grafana по адресу `http://notebook.iri1968.dpdns.org`.
2.  Войдите (логин `admin`, пароль можно узнать с помощью `./connect_grafana.sh`).
3.  Перейдите в раздел `Dashboards` -> `New` -> `Import`.
4.  Нажмите `Upload JSON file` и выберите файл `wireguard-dashboard.json` из этого репозитория.
5.  В разделе `Options` выберите `Prometheus` в качестве источника данных.
6.  Нажмите `Import`.

### **Шаг 7: Локальный доступ к Grafana (опционально)**

Если вам нужен временный доступ к Grafana без настройки роутера или DNS, вы можете использовать скрипт `connect_grafana.sh`.

```bash
./connect_grafana.sh
```
Этот скрипт автоматически пробросит порт 3000 вашего локального компьютера на Grafana, и вы сможете получить доступ по адресу `http://localhost:3000`. В терминале, где запущен скрипт, также будет отображен пароль администратора.
